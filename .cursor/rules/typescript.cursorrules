{
  "name": "TypeScript Rules",
  "description": "Rules for TypeScript usage across the project",
  "rules": [
    "All files should be written in TypeScript (.ts or .tsx).",
    "Use explicit types/interfaces for all component props, API responses, and function parameters.",
    "Use zod for schema validation of API responses.",
    "Never use 'any' type; prefer unknown + proper type narrowing.",
    "Type guards must be used for dynamic data or API responses."
  ]
}
